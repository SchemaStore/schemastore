# yaml-language-server: $schema=../../schemas/json/github-workflow.json
on: push
permissions:
  id-token: none
jobs:
  example:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
    steps:
      - run: echo example
