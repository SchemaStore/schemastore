# yaml-language-server: $schema=../../schemas/json/kustomization.json
kind: Kustomization
sortOptions:
  order: legacy
  legacySortOptions:
    orderFirst:
      - Namespace
    orderLast:
      - Deployment
