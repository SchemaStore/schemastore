# yaml-language-server: $schema=../../schemas/json/allstar-action.json
baseConfig: acme/.allstar
action: issue
groups:
  - name: common-workflows
    rules:
      - name: require-pinned-actions
        method: require
        priority: high
        actions:
          - name: actions/checkout
            version: 'v4'
