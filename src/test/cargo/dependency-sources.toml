#:schema ../../schemas/json/cargo.json
[package]
name = "dependency-sources"
version = "0.1.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
regex = { git = "https://github.com/rust-lang/regex.git", version = "1" }
local = { path = "../local", version = "1" }
shared = { workspace = true }
