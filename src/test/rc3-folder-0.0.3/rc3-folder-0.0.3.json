{
    "$schema": "http://localhost:8000/rc3-folder-0.0.3.json",
    "comment": "Example folder level auth that all requests in this folder & sub-folders will inherit by default",
    "auth": {
        "type": "basic",
        "username": "gary",
        "password": "{{ password }}"
    }
}