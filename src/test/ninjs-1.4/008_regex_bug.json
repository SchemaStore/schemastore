{
  "$standard": {
    "name": "ninjs",
    "schema": "http://www.iptc.org/std/ninjs/ninjs-schema_1.4.json",
    "version": "1.4"
  },
  "associations": {
    "_#$^-éééash-é™£": {
      "uri": "urn:embedded-association"
    }
  },
  "body_test-bad-characters-#$^-éééash-é™£": "This is actually valid in ninjs 1.0, 1.1, 1.2, 1.3 and 1.4, due to a bug in the patternProperties regex. But we can't change it without potentially breaking some implementations so the fix will have to wait for 2.0",
  "description_test-bad-characters-#$^-é™£": "This is actually valid in ninjs 1.0, 1.1, 1.2, 1.3 and 1.4, due to a bug in the patternProperties regex. But we can't change it without potentially breaking some implementations so the fix will have to wait for 2.0",
  "headline_test-bad-characters-#$^-é™£": "This is actually valid in ninjs 1.0, 1.1, 1.2, 1.3 and 1.4, due to a bug in the patternProperties regex. But we can't change it without potentially breaking some implementations so the fix will have to wait for 2.0",
  "place": [
    {
      "geometry_test-bad-characters-#$^-éééash-é™£": {},
      "name": "The below geometry field is actually valid in ninjs 1.0, 1.1, 1.2, 1.3 and 1.4, due to a bug in the patternProperties regex. But we can't change it without potentially breaking some implementations so the fix will have to wait for 2.0"
    }
  ],
  "renditions": {
    "a#$^-éééash-é™£": {}
  },
  "uri": "urn:008-regex-bug"
}
