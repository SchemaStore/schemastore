[project]
name = 'foo'
version = '1.0'

# Up to tools to report content type mismatch or
# unsupported format, not pyproject spec
[project.readme]
text = 'This is not readme'
content-type = "text/postscript; charset=us-ascii"
