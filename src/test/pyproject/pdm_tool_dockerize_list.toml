#:schema ../../schemas/json/pyproject.json
[tool.pdm.dockerize]
include = ["*"]
exclude = ["some-script"]
include_bins = ["*"]
exclude_bins = ["some-bin"]
env_file = "my.env"

[tool.pdm.dockerize.env]
VAR = "value"
