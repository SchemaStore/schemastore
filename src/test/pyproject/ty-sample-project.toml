#:schema ../../schemas/json/pyproject.json
[tool.ty.src]
root = "src"

[tool.ty.rules]
division-by-zero = "error"

[tool.ty.terminal]
output-format = "concise"

[tool.ty.environment]
python-version = "3.13"
