# yaml-language-server: $schema=../../schemas/json/circleciconfig.json
version: 2.1
jobs:
  prepare:
    docker:
      - image: image
    steps:
      - setup_remote_docker:
          version: '20.10.24'
