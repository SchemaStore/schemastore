#:schema ../../schemas/json/tox.json
env_list = [
  { product = [["py39", "py310"], ["django42", "django50"]], exclude = ["py39-django50"] }
]
no_package = true

[env_run_base]
commands = [["python", "-m", "pytest"]]
