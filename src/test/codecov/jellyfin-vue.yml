# yaml-language-server: $schema=../../schemas/json/codecov.json
coverage:
  status:
    project:
      default:
        threshold: 1%
        if_not_found: success # no commit found? still set a success
    patch: off
