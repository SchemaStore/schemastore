{
  "name": "my-app",
  "nodemonConfig": {
    "exec": "npm run build --workspaces --if-present",
    "ext": "*",
    "ignore": ["*.min.*"],
    "watch": ["src"]
  },
  "scripts": {
    "watch": "nodemon"
  },
  "version": "1.0.0"
}
