#:schema ../../schemas/json/skillcheck.json
max-tokens = 8000
max-lines = 750
min-desc-score = 25
strict-vscode = true
strict-cursor = true
target-agent = "all"
semantic = true

[frontmatter]
extension_fields = ["license", "metadata"]
