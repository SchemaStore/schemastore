# yaml-language-server: $schema=../../schemas/json/tmlanguage.json

name: My Grammar
scopeName: source.lang
fileTypes:
  - lang

patterns:
  - include: '#comments'

repository:
  comments:
    patterns:
      - name: comment.lang
        match: ^#.*$
