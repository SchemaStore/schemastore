#:schema ../../schemas/json/cargo-config.json
# Values such as profile inheritance and source locations may come from parent configs.
[profile.custom]
opt-level = 2
[source.parent-source]
replace-with = "vendor"
