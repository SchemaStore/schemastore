# yaml-language-server: $schema=../../schemas/json/amplify.json
version: 1
applications:
  - appRoot: 'frontend'
    frontend:
      phases:
        preBuild:
          commands:
            - yarn install
        build:
          commands:
            - yarn build
      artifacts:
        baseDirectory: build
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**
    backend:
      someKey: someValue
