{
  "environments": {
    "production": {
      "configuration": {
        "monitoring": {
          "alerts": ["cpu_usage", "memory_usage", "error_rate", "latency"],
          "enabled": true
        }
      }
    }
  },
  "id": "service-monitored",
  "workflow_dispatch": "deploy.yml"
}
