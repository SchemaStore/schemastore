{
  "$standard": {
    "name": "ninjs",
    "version": "1.3",
    "schema": "http://www.iptc.org/std/ninjs/ninjs-schema_1.3.json"
  },
  "associations": {
    "_#$^-éééash-é™£": {
      "uri": "urn:embedded-association"
    }
  },
  "body_test-bad-characters-#$^-éééash-é™£": "This is actually valid in ninjs 1.0, 1.1, 1.2 and 1.3, due to a bug in the patternProperties regex. But we can't change it without potentially breaking some implementations so the fix will have to wait for 2.0",
  "description_test-bad-characters-#$^-é™£": "This is actually valid in ninjs 1.0, 1.1, 1.2 and 1.3, due to a bug in the patternProperties regex. But we can't change it without potentially breaking some implementations so the fix will have to wait for 2.0",
  "headline_test-bad-characters-#$^-é™£": "This is actually valid in ninjs 1.0, 1.1, 1.2 and 1.3, due to a bug in the patternProperties regex. But we can't change it without potentially breaking some implementations so the fix will have to wait for 2.0",
  "place": [
    {
      "name": "The below geometry field is actually valid in ninjs 1.0, 1.1, 1.2 and 1.3, due to a bug in the patternProperties regex. But we can't change it without potentially breaking some implementations so the fix will have to wait for 2.0",
      "geometry_test-bad-characters-#$^-éééash-é™£": {}
    }
  ],
  "renditions": {
    "a#$^-éééash-é™£": {}
  },
  "uri": "urn:008-regex-bug"
}
