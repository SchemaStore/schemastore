# yaml-language-server: $schema=../../schemas/json/one-changelog-schema-0.1.json
---
name: Short description of change
jira: UNITYONE-0000
tag: PATCH
apiChange: false
integrationApiChange: false
description: |2-

  Detailed description of change. (optional)

  Can be multiline.

  ```
  You can also use code
  blocks here!
  ```

  Some other useful info:

  * `tag` field can contain one of values: MAJOR, MINOR, PATCH
  * `apiChange` indicates changes in front (app) apis
  * `integrationApiChange` indicates changes in integration apis
