{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "http://json.schemastore.org/cloudworkflows",
  "title": "Google Cloud Workflows config file",
  "description": "Orchestrate Workflows consisting of Google Cloud APIs, SaaS APIs or private API endpoints.",
  "type": "array",
  "items": {
    "$ref": "#/definitions/steps"
  },
  "definitions": {
    "steps": {
      "type": "object",
      "items": {
        "$ref": "#/definitions/step"
      }
    },
    "step": {
      "type": "object",
      "minProperties": 1,
      "maxProperties": 1,
      "additionalProperties": false,
      "properties": {
        "call": {
          "type": "string",
          "enum": [
            "http.get",
            "http.post",
            "http.request"
          ]
        },
        "args": {
          "type": "object",
          "properties": {
            "url": {
              "type": "string",
              "format": "uri"
            },
            "method": {
              "type": "string",
              "enum": [
                "GET",
                "HEAD",
                "POST",
                "PUT",
                "DELETE",
                "CONNECT",
                "OPTIONS",
                "TRACE",
                "PATCH"
              ]
            },
            "header": {
              "type": "object"
            },
            "body": {
              "type": "object"
            },
            "query": {
              "type": "object"
            },
            "auth": {
              "properties": {
                "type": {
                  "enum": ["OIDC", "OAuth2"]
                }
              }
            },
            "timeout": {
              "type": "number"
            }
          }
        },
        "assign": {
          "type": "object"
        },
        "result": {
          "type": "string"
        },
        "return": {
        },
        "switch": {
          "properties": {
            "steps": {
              "$ref": "#/definitions/steps"
            }
          }
        },
        "next": {
          "type": "string"
        },
        "try": {
          "properties": {
            "steps": {
              "$ref": "#/definitions/steps"
            }
          }
        },
        "retry": {
          "properties": {
            "predicate": {
              "type": "string"
            },
            "max_retries": {
              "type": "integer"
            },
            "backoff": {
              "properties": {
                "initial_delay": {
                  "type": "integer"
                },
                "max_delay": {
                  "type": "integer"
                },
                "multiplier": {
                  "type": "integer"
                }
              }
            }
          }
        },
        "except": {
          "$ref": "#/definitions/steps"
        }
      }
    },
    "as": {
      "type": "string",
      "steps": {
        "$ref": "#/definitions/steps"
      }
    },
    "params": {
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  }
}