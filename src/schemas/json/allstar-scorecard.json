{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://json.schemastore.org/allstar-scorecard.json",
  "title": "Allstar Scorecard policy configuration",
  "type": "object",
  "definitions": {
    "OrgConfig": {
      "type": "object",
      "properties": {
        "optConfig": {
          "$ref": "#/definitions/OrgOptConfig"
        },
        "action": {
          "description": "Which action to take. Default log; other options include issue.",
          "type": "string",
          "default": "log"
        },
        "checks": {
          "type": "array",
          "description": "List of check names to run from OpenSSF Scorecard. Must match the name used in the check's registration.",
          "items": {
            "type": "string"
          }
        },
        "threshold": {
          "description": "Score threshold that checks must meet to pass the policy. Default is the maximum result score defined by scorecard.",
          "type": "integer"
        },
        "upload": {
          "$ref": "#/definitions/UploadConfig"
        },
        "baseConfig": {
          "$ref": "#/definitions/BaseConfig"
        }
      },
      "required": ["optConfig", "action"],
      "additionalProperties": false
    },
    "RepoConfig": {
      "type": "object",
      "properties": {
        "optConfig": {
          "$ref": "#/definitions/RepoOptConfig"
        },
        "action": {
          "description": "Override for org-level action when present.",
          "type": "string"
        },
        "checks": {
          "description": "Override for org-level checks when present.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "null"
            }
          ]
        },
        "threshold": {
          "description": "Override for org-level threshold when present.",
          "type": "integer"
        },
        "upload": {
          "$ref": "#/definitions/UploadConfig"
        }
      },
      "additionalProperties": false,
      "required": []
    },
    "UploadConfig": {
      "type": "object",
      "description": "Configures evidence upload. When SARIF is enabled, results are sent to GitHub's Code Scanning API, and the GitHub App must have the security_events: write permission (see https://github.com/ossf/allstar/blob/main/operator.md).",
      "properties": {
        "sarif": {
          "type": "boolean",
          "description": "Whether to upload SARIF results to GitHub's Code Scanning API. The GitHub App needs the security_events: write permission when enabled."
        }
      },
      "additionalProperties": false,
      "required": []
    },
    "OrgOptConfig": {
      "type": "object",
      "description": "OptConfig contains the opt in/out configuration.",
      "properties": {
        "optOutStrategy": {
          "$ref": "#/definitions/OptOutStrategy"
        },
        "optInRepos": {
          "$ref": "#/definitions/OptInRepos"
        },
        "optOutRepos": {
          "$ref": "#/definitions/OptOutRepos"
        },
        "optOutPrivateRepos": {
          "$ref": "#/definitions/OptOutPrivateRepos"
        },
        "optOutPublicRepos": {
          "$ref": "#/definitions/OptOutPublicRepos"
        },
        "optOutArchivedRepos": {
          "$ref": "#/definitions/OptOutArchivedRepos"
        },
        "optOutForkedRepos": {
          "$ref": "#/definitions/OptOutForkedRepos"
        },
        "disableRepoOverride": {
          "$ref": "#/definitions/DisableRepoOverride"
        }
      },
      "required": [],
      "additionalProperties": false
    },
    "DisableRepoOverride": {
      "type": "boolean",
      "description": "DisableRepoOverride : set to true to disallow repos from opt-in/out in their config."
    },
    "OptOutForkedRepos": {
      "type": "boolean",
      "description": "OptOutForkedRepos : set to true to opt-out forked repositories."
    },
    "OptOutArchivedRepos": {
      "type": "boolean",
      "description": "OptOutArchivedRepos : set to true to opt-out archived repositories."
    },
    "OptOutPublicRepos": {
      "type": "boolean",
      "description": "OptOutPublicRepos : set to true to not access public repos."
    },
    "OptOutPrivateRepos": {
      "type": "boolean",
      "description": "OptOutPrivateRepos : set to true to not access private repos."
    },
    "OptOutRepos": {
      "type": "array",
      "description": "OptOutRepos is the list of repos to opt-out when in opt-out strategy.",
      "items": {
        "type": "string"
      }
    },
    "OptInRepos": {
      "type": "array",
      "description": "OptInRepos is the list of repos to opt-in when in opt-in strategy.",
      "items": {
        "type": "string"
      }
    },
    "OptOutStrategy": {
      "type": "boolean",
      "description": "OptOutStrategy : set to true to change from opt-in to opt-out."
    },
    "RepoOptConfig": {
      "type": "object",
      "description": "Repository-level opt-in and opt-out settings.",
      "properties": {
        "optIn": {
          "type": "boolean",
          "description": "Opt in this repository when the organization uses opt-in strategy."
        },
        "optOut": {
          "type": "boolean",
          "description": "Opt out this repository when the organization uses opt-out strategy."
        }
      },
      "required": [],
      "additionalProperties": false
    },
    "BaseConfig": {
      "type": "string",
      "description": "The GitHub owner/repository containing the base configuration. The file at the same path is merged first, then values in this file override it. See https://github.com/ossf/allstar#org-level-base-and-merge-configuration-location.",
      "pattern": "^[^/]+/[^/]+$",
      "maxLength": 140
    }
  },
  "anyOf": [
    {
      "$ref": "#/definitions/OrgConfig"
    },
    {
      "$ref": "#/definitions/RepoConfig"
    }
  ]
}
