{
  "title": "JSON schema for file system client-side library",
  "$schema": "http://json-schema.org/draft-04/schema#",

  "type": "object",
  "required": [ "files", "path", "provider" ],
  "additionalProperties": false,

  "properties": {
    "files": {
      "description": "The absolute path or URI to the files to copy to the project.",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "path": {
      "description": "The relative folder path from this config file to the location where the library files should be copied to.",
      "type": "string",
      "minLength": 1
    },
    "provider": {
      "description": "The unique identifier of the provider",
      "enum": [ "filesystem" ]
    }
  }
}