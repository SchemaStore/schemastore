{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://json.schemastore.org/partial-poetry-plugin-dotenv.json",
  "$comment": "tool.poetry.plugins.dotenv table in pyproject.toml",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "ignore": {
      "type": "string",
      "description": "Flag that prevents the plugin from loading the dotenv file."
    },
    "location": {
      "type": "string",
      "description": "Path to the dotenv file. It can be both absolute and relative."
    }
  }
}
