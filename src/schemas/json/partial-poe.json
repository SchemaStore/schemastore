{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://json.schemastore.org/partial-poe.json",
  "$comment": "Generated by poethepoet 0.47.0. Do not edit by hand.",
  "additionalProperties": false,
  "definitions": {
    "args_item": {
      "additionalProperties": false,
      "allOf": [
        {
          "if": {
            "properties": { "type": { "const": "boolean" } },
            "required": ["type"]
          },
          "then": {
            "properties": {
              "default": {
                "anyOf": [
                  { "type": "boolean" },
                  {
                    "pattern": "^(\\s*([Tt]([Rr][Uu][Ee])?|[Ff]([Aa][Ll][Ss][Ee])?|0|1)?\\s*|.*\\$\\{.*)$",
                    "type": "string"
                  }
                ]
              }
            }
          }
        }
      ],
      "properties": {
        "choices": {
          "anyOf": [
            { "items": { "type": "string" }, "type": "array" },
            { "items": { "type": "number" }, "type": "array" },
            { "items": { "type": "integer" }, "type": "array" }
          ],
          "description": "Constrain the accepted values for an argument to a fixed set."
        },
        "default": {
          "anyOf": [
            { "type": "string" },
            { "type": "integer" },
            { "type": "number" },
            { "type": "boolean" }
          ],
          "description": "The default value for the argument when not provided."
        },
        "help": {
          "default": "",
          "description": "A short description of the argument to include in the documentation of the\ntask.",
          "type": "string"
        },
        "multiple": {
          "anyOf": [{ "type": "boolean" }, { "type": "integer" }],
          "default": false,
          "description": "Indicates if multiple values are allowed for the argument. If an integer is\ngiven, exactly that many values are expected."
        },
        "name": {
          "description": "The name of the argument.",
          "type": "string"
        },
        "options": {
          "description": "A list of options to be provided along with the argument.",
          "items": { "type": "string" },
          "type": "array"
        },
        "positional": {
          "anyOf": [{ "type": "boolean" }, { "type": "string" }],
          "default": false,
          "description": "Indicates if the argument is positional. If a string is provided, it is used\nas the dest name for the argument in argparse."
        },
        "required": {
          "default": false,
          "description": "Indicates if the argument is required.",
          "type": "boolean"
        },
        "type": {
          "default": "string",
          "description": "The type of the argument.",
          "enum": ["string", "float", "integer", "boolean"],
          "type": "string"
        }
      },
      "required": ["name"],
      "type": "object"
    },
    "args_item_no_name": {
      "additionalProperties": false,
      "allOf": [
        {
          "if": {
            "properties": { "type": { "const": "boolean" } },
            "required": ["type"]
          },
          "then": {
            "properties": {
              "default": {
                "anyOf": [
                  { "type": "boolean" },
                  {
                    "pattern": "^(\\s*([Tt]([Rr][Uu][Ee])?|[Ff]([Aa][Ll][Ss][Ee])?|0|1)?\\s*|.*\\$\\{.*)$",
                    "type": "string"
                  }
                ]
              }
            }
          }
        }
      ],
      "properties": {
        "choices": {
          "anyOf": [
            { "items": { "type": "string" }, "type": "array" },
            { "items": { "type": "number" }, "type": "array" },
            { "items": { "type": "integer" }, "type": "array" }
          ],
          "description": "Constrain the accepted values for an argument to a fixed set."
        },
        "default": {
          "anyOf": [
            { "type": "string" },
            { "type": "integer" },
            { "type": "number" },
            { "type": "boolean" }
          ],
          "description": "The default value for the argument when not provided."
        },
        "help": {
          "default": "",
          "description": "A short description of the argument to include in the documentation of the\ntask.",
          "type": "string"
        },
        "multiple": {
          "anyOf": [{ "type": "boolean" }, { "type": "integer" }],
          "default": false,
          "description": "Indicates if multiple values are allowed for the argument. If an integer is\ngiven, exactly that many values are expected."
        },
        "options": {
          "description": "A list of options to be provided along with the argument.",
          "items": { "type": "string" },
          "type": "array"
        },
        "positional": {
          "anyOf": [{ "type": "boolean" }, { "type": "string" }],
          "default": false,
          "description": "Indicates if the argument is positional. If a string is provided, it is used\nas the dest name for the argument in argparse."
        },
        "required": {
          "default": false,
          "description": "Indicates if the argument is required.",
          "type": "boolean"
        },
        "type": {
          "default": "string",
          "description": "The type of the argument.",
          "enum": ["string", "float", "integer", "boolean"],
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "args_option": {
      "anyOf": [
        {
          "items": {
            "anyOf": [
              { "type": "string" },
              { "$ref": "#/definitions/args_item" }
            ]
          },
          "type": "array"
        },
        {
          "additionalProperties": { "$ref": "#/definitions/args_item_no_name" },
          "type": "object"
        }
      ]
    },
    "cmd_task": {
      "additionalProperties": false,
      "if": {
        "properties": { "use_exec": { "const": true } },
        "required": ["use_exec"]
      },
      "properties": {
        "args": { "$ref": "#/definitions/args_option" },
        "capture_stdout": {
          "description": "Redirects the task output to a file with the given path. Supports\nenvironment variable interpolation.",
          "type": "string"
        },
        "cmd": {
          "description": "Executes a single command as a subprocess without a shell. Supports glob\npatterns for filesystem paths, parameter expansion of environment variable\nor private variables.",
          "examples": [
            "rm -rf ./**/*.pyc",
            "echo Hello ${USER}",
            "echo Hello \\${USER}"
          ],
          "title": "Command to execute",
          "type": "string"
        },
        "cwd": {
          "description": "Specify the current working directory that this task should run with. This\ncan be a relative path from the project root or an absolute path, and\nenvironment variables can be used in the format ${VAR_NAME}.",
          "pattern": "\\S",
          "type": "string"
        },
        "deps": {
          "description": "A list of task invocations that will be executed before this one. Each item\nin the list is a reference to another task defined within the tasks object.",
          "items": { "type": "string" },
          "type": "array"
        },
        "empty_glob": {
          "default": "pass",
          "description": "Determines how to handle glob patterns with no matches. The default is\n'pass', which causes unmatched patterns to be passed through to the command\n(just like in bash). Setting it to 'null' will replace an unmatched pattern\nwith nothing, and setting it to 'fail' will cause the task to fail with an\nerror if there are no matches.",
          "enum": ["pass", "null", "fail"],
          "type": "string"
        },
        "env": { "$ref": "#/definitions/env_option" },
        "envfile": { "$ref": "#/definitions/envfile_option" },
        "executor": { "$ref": "#/definitions/executor_task_option" },
        "help": {
          "description": "Help text to be displayed next to the task name in the documentation when\npoe is run without specifying a task.",
          "type": "string"
        },
        "ignore_fail": {
          "anyOf": [
            { "type": "boolean" },
            { "items": { "type": "integer" }, "type": "array" }
          ],
          "default": false,
          "description": "Return exit code 0 even if the task fails, or specify a list of task exit\ncodes to ignore."
        },
        "use_exec": {
          "default": false,
          "description": "Specify that this task should be executed in the same process, instead of\nas a subprocess. Note: This feature has limitations, such as not being\ncompatible with tasks that are referenced by other tasks and not working on\nWindows.",
          "type": "boolean"
        },
        "uses": {
          "additionalProperties": { "type": "string" },
          "description": "Allows this task to use the output of other tasks which are executed first.\nThe values are references to the names of the tasks, and the keys are\nenvironment variables by which the results of those tasks will be\naccessible in this task.",
          "type": "object"
        },
        "verbosity": {
          "description": "Specify the verbosity level for this task, from -2 (least verbose) to 2\n(most verbose), overriding the project level verbosity setting, which\ndefaults to 0.",
          "enum": [-2, -1, 0, 1, 2],
          "type": "integer"
        }
      },
      "required": ["cmd"],
      "then": { "properties": { "capture_stdout": false } },
      "type": "object"
    },
    "cmd_task_with_case": {
      "additionalProperties": false,
      "if": {
        "properties": { "use_exec": { "const": true } },
        "required": ["use_exec"]
      },
      "properties": {
        "args": { "$ref": "#/definitions/args_option" },
        "capture_stdout": {
          "description": "Redirects the task output to a file with the given path. Supports\nenvironment variable interpolation.",
          "type": "string"
        },
        "case": {
          "anyOf": [
            {
              "anyOf": [
                { "type": "string" },
                { "type": "integer" },
                { "type": "boolean" }
              ]
            },
            {
              "items": {
                "anyOf": [
                  { "type": "string" },
                  { "type": "integer" },
                  { "type": "boolean" }
                ]
              },
              "type": "array"
            }
          ]
        },
        "cmd": {
          "description": "Executes a single command as a subprocess without a shell. Supports glob\npatterns for filesystem paths, parameter expansion of environment variable\nor private variables.",
          "examples": [
            "rm -rf ./**/*.pyc",
            "echo Hello ${USER}",
            "echo Hello \\${USER}"
          ],
          "title": "Command to execute",
          "type": "string"
        },
        "cwd": {
          "description": "Specify the current working directory that this task should run with. This\ncan be a relative path from the project root or an absolute path, and\nenvironment variables can be used in the format ${VAR_NAME}.",
          "pattern": "\\S",
          "type": "string"
        },
        "deps": {
          "description": "A list of task invocations that will be executed before this one. Each item\nin the list is a reference to another task defined within the tasks object.",
          "items": { "type": "string" },
          "type": "array"
        },
        "empty_glob": {
          "default": "pass",
          "description": "Determines how to handle glob patterns with no matches. The default is\n'pass', which causes unmatched patterns to be passed through to the command\n(just like in bash). Setting it to 'null' will replace an unmatched pattern\nwith nothing, and setting it to 'fail' will cause the task to fail with an\nerror if there are no matches.",
          "enum": ["pass", "null", "fail"],
          "type": "string"
        },
        "env": { "$ref": "#/definitions/env_option" },
        "envfile": { "$ref": "#/definitions/envfile_option" },
        "executor": { "$ref": "#/definitions/executor_task_option" },
        "help": {
          "description": "Help text to be displayed next to the task name in the documentation when\npoe is run without specifying a task.",
          "type": "string"
        },
        "ignore_fail": {
          "anyOf": [
            { "type": "boolean" },
            { "items": { "type": "integer" }, "type": "array" }
          ],
          "default": false,
          "description": "Return exit code 0 even if the task fails, or specify a list of task exit\ncodes to ignore."
        },
        "use_exec": {
          "default": false,
          "description": "Specify that this task should be executed in the same process, instead of\nas a subprocess. Note: This feature has limitations, such as not being\ncompatible with tasks that are referenced by other tasks and not working on\nWindows.",
          "type": "boolean"
        },
        "uses": {
          "additionalProperties": { "type": "string" },
          "description": "Allows this task to use the output of other tasks which are executed first.\nThe values are references to the names of the tasks, and the keys are\nenvironment variables by which the results of those tasks will be\naccessible in this task.",
          "type": "object"
        },
        "verbosity": {
          "description": "Specify the verbosity level for this task, from -2 (least verbose) to 2\n(most verbose), overriding the project level verbosity setting, which\ndefaults to 0.",
          "enum": [-2, -1, 0, 1, 2],
          "type": "integer"
        }
      },
      "required": ["cmd"],
      "then": { "properties": { "capture_stdout": false } },
      "type": "object"
    },
    "env_default": {
      "additionalProperties": false,
      "properties": {
        "default": {
          "description": "A default value for an environment variable that will be used only if the\nvariable is not already set.",
          "type": "string"
        }
      },
      "required": ["default"],
      "type": "object"
    },
    "env_option": {
      "additionalProperties": {
        "anyOf": [{ "type": "string" }, { "$ref": "#/definitions/env_default" }]
      },
      "type": "object"
    },
    "envfile_full": {
      "additionalProperties": false,
      "properties": {
        "expected": {
          "anyOf": [
            { "type": "string" },
            { "items": { "type": "string" }, "type": "array" }
          ],
          "description": "Provide one or more env files to be loaded before running this task. Emit a\nwarning if any specified envfile is missing."
        },
        "optional": {
          "anyOf": [
            { "type": "string" },
            { "items": { "type": "string" }, "type": "array" }
          ],
          "description": "Provide one or more env files to be loaded before running this task. Do not\nemit a warning even if a specified envfile is missing."
        }
      },
      "required": [],
      "type": "object"
    },
    "envfile_option": {
      "oneOf": [
        { "type": "string" },
        { "$ref": "#/definitions/envfile_full" },
        {
          "items": {
            "oneOf": [
              { "type": "string" },
              { "$ref": "#/definitions/envfile_full" }
            ]
          },
          "type": "array"
        }
      ]
    },
    "executor_auto": {
      "additionalProperties": false,
      "properties": { "type": { "enum": ["auto"], "type": "string" } },
      "required": ["type"],
      "type": "object"
    },
    "executor_auto_partial": {
      "additionalProperties": false,
      "properties": {},
      "type": "object"
    },
    "executor_option": {
      "oneOf": [
        {
          "enum": ["auto", "poetry", "simple", "uv", "virtualenv"],
          "type": "string"
        },
        { "$ref": "#/definitions/executor_poetry" },
        { "$ref": "#/definitions/executor_simple" },
        { "$ref": "#/definitions/executor_uv" },
        { "$ref": "#/definitions/executor_virtualenv" },
        { "$ref": "#/definitions/executor_auto" }
      ]
    },
    "executor_partial": {
      "anyOf": [
        { "$ref": "#/definitions/executor_poetry_partial" },
        { "$ref": "#/definitions/executor_simple_partial" },
        { "$ref": "#/definitions/executor_uv_partial" },
        { "$ref": "#/definitions/executor_virtualenv_partial" },
        { "$ref": "#/definitions/executor_auto_partial" }
      ]
    },
    "executor_poetry": {
      "additionalProperties": false,
      "properties": { "type": { "enum": ["poetry"], "type": "string" } },
      "required": ["type"],
      "type": "object"
    },
    "executor_poetry_partial": {
      "additionalProperties": false,
      "properties": {},
      "type": "object"
    },
    "executor_simple": {
      "additionalProperties": false,
      "properties": { "type": { "enum": ["simple"], "type": "string" } },
      "required": ["type"],
      "type": "object"
    },
    "executor_simple_partial": {
      "additionalProperties": false,
      "properties": {},
      "type": "object"
    },
    "executor_task_option": {
      "oneOf": [
        {
          "enum": ["auto", "poetry", "simple", "uv", "virtualenv"],
          "type": "string"
        },
        { "$ref": "#/definitions/executor_poetry" },
        { "$ref": "#/definitions/executor_simple" },
        { "$ref": "#/definitions/executor_uv" },
        { "$ref": "#/definitions/executor_virtualenv" },
        { "$ref": "#/definitions/executor_auto" },
        { "$ref": "#/definitions/executor_partial" }
      ]
    },
    "executor_uv": {
      "additionalProperties": false,
      "properties": {
        "exact": {
          "default": false,
          "description": "Perform an exact sync, removing extraneous packages from the environment.",
          "type": "boolean"
        },
        "extra": {
          "anyOf": [
            { "type": "string" },
            { "items": { "type": "string" }, "type": "array" }
          ],
          "description": "Include optional dependencies from the specified extra name."
        },
        "frozen": {
          "default": false,
          "description": "Run without updating the uv.lock file.",
          "type": "boolean"
        },
        "group": {
          "anyOf": [
            { "type": "string" },
            { "items": { "type": "string" }, "type": "array" }
          ],
          "description": "Include dependencies from the specified dependency group."
        },
        "isolated": {
          "default": false,
          "description": "Run the command in an isolated virtual environment.",
          "type": "boolean"
        },
        "locked": {
          "default": false,
          "description": "Assert that the uv.lock file is up to date; fail if it would need to be\nupdated.",
          "type": "boolean"
        },
        "no-group": {
          "anyOf": [
            { "type": "string" },
            { "items": { "type": "string" }, "type": "array" }
          ],
          "description": "Disable the specified dependency group."
        },
        "no-project": {
          "default": false,
          "description": "Avoid discovering the project or workspace.",
          "type": "boolean"
        },
        "no-sync": {
          "default": false,
          "description": "Avoid syncing the virtual environment.",
          "type": "boolean"
        },
        "python": {
          "description": "The Python interpreter to use for the run environment.",
          "type": "string"
        },
        "type": { "enum": ["uv"], "type": "string" },
        "with": {
          "anyOf": [
            { "type": "string" },
            { "items": { "type": "string" }, "type": "array" }
          ],
          "description": "Run with the given packages installed."
        }
      },
      "required": ["type"],
      "type": "object"
    },
    "executor_uv_partial": {
      "additionalProperties": false,
      "properties": {
        "exact": {
          "default": false,
          "description": "Perform an exact sync, removing extraneous packages from the environment.",
          "type": "boolean"
        },
        "extra": {
          "anyOf": [
            { "type": "string" },
            { "items": { "type": "string" }, "type": "array" }
          ],
          "description": "Include optional dependencies from the specified extra name."
        },
        "frozen": {
          "default": false,
          "description": "Run without updating the uv.lock file.",
          "type": "boolean"
        },
        "group": {
          "anyOf": [
            { "type": "string" },
            { "items": { "type": "string" }, "type": "array" }
          ],
          "description": "Include dependencies from the specified dependency group."
        },
        "isolated": {
          "default": false,
          "description": "Run the command in an isolated virtual environment.",
          "type": "boolean"
        },
        "locked": {
          "default": false,
          "description": "Assert that the uv.lock file is up to date; fail if it would need to be\nupdated.",
          "type": "boolean"
        },
        "no-group": {
          "anyOf": [
            { "type": "string" },
            { "items": { "type": "string" }, "type": "array" }
          ],
          "description": "Disable the specified dependency group."
        },
        "no-project": {
          "default": false,
          "description": "Avoid discovering the project or workspace.",
          "type": "boolean"
        },
        "no-sync": {
          "default": false,
          "description": "Avoid syncing the virtual environment.",
          "type": "boolean"
        },
        "python": {
          "description": "The Python interpreter to use for the run environment.",
          "type": "string"
        },
        "with": {
          "anyOf": [
            { "type": "string" },
            { "items": { "type": "string" }, "type": "array" }
          ],
          "description": "Run with the given packages installed."
        }
      },
      "type": "object"
    },
    "executor_virtualenv": {
      "additionalProperties": false,
      "properties": {
        "location": {
          "description": "Specifies the location of the virtualenv relative to the parent directory.\nRelevant when 'type' is set to 'virtualenv'.",
          "type": "string"
        },
        "type": { "enum": ["virtualenv"], "type": "string" }
      },
      "required": ["type"],
      "type": "object"
    },
    "executor_virtualenv_partial": {
      "additionalProperties": false,
      "properties": {
        "location": {
          "description": "Specifies the location of the virtualenv relative to the parent directory.\nRelevant when 'type' is set to 'virtualenv'.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "expr_task": {
      "additionalProperties": false,
      "if": {
        "properties": { "use_exec": { "const": true } },
        "required": ["use_exec"]
      },
      "properties": {
        "args": { "$ref": "#/definitions/args_option" },
        "assert": {
          "anyOf": [{ "type": "boolean" }, { "type": "integer" }],
          "default": false,
          "description": "A boolean indicating if the task will fail when the result of the\nexpression is falsy. If an integer is given, the task will exit with that\ncode when the result is falsy."
        },
        "capture_stdout": {
          "description": "Redirects the task output to a file with the given path. Supports\nenvironment variable interpolation.",
          "type": "string"
        },
        "cwd": {
          "description": "Specify the current working directory that this task should run with. This\ncan be a relative path from the project root or an absolute path, and\nenvironment variables can be used in the format ${VAR_NAME}.",
          "pattern": "\\S",
          "type": "string"
        },
        "deps": {
          "description": "A list of task invocations that will be executed before this one. Each item\nin the list is a reference to another task defined within the tasks object.",
          "items": { "type": "string" },
          "type": "array"
        },
        "env": { "$ref": "#/definitions/env_option" },
        "envfile": { "$ref": "#/definitions/envfile_option" },
        "executor": { "$ref": "#/definitions/executor_task_option" },
        "expr": {
          "description": "Evaluates a Python expression to produce a result. Can reference\nenvironment variables and arguments, and leverage most python builtins.",
          "type": "string"
        },
        "help": {
          "description": "Help text to be displayed next to the task name in the documentation when\npoe is run without specifying a task.",
          "type": "string"
        },
        "ignore_fail": {
          "anyOf": [
            { "type": "boolean" },
            { "items": { "type": "integer" }, "type": "array" }
          ],
          "default": false,
          "description": "Return exit code 0 even if the task fails, or specify a list of task exit\ncodes to ignore."
        },
        "imports": {
          "description": "A list of Python modules to be imported for use in the expression.",
          "items": { "type": "string" },
          "type": "array"
        },
        "use_exec": {
          "default": false,
          "description": "Specify that this task should be executed in the same process, instead of\nas a subprocess. Note: This feature has limitations, such as not being\ncompatible with tasks that are referenced by other tasks and not working on\nWindows.",
          "type": "boolean"
        },
        "uses": {
          "additionalProperties": { "type": "string" },
          "description": "Allows this task to use the output of other tasks which are executed first.\nThe values are references to the names of the tasks, and the keys are\nenvironment variables by which the results of those tasks will be\naccessible in this task.",
          "type": "object"
        },
        "verbosity": {
          "description": "Specify the verbosity level for this task, from -2 (least verbose) to 2\n(most verbose), overriding the project level verbosity setting, which\ndefaults to 0.",
          "enum": [-2, -1, 0, 1, 2],
          "type": "integer"
        }
      },
      "required": ["expr"],
      "then": { "properties": { "capture_stdout": false } },
      "type": "object"
    },
    "expr_task_with_case": {
      "additionalProperties": false,
      "if": {
        "properties": { "use_exec": { "const": true } },
        "required": ["use_exec"]
      },
      "properties": {
        "args": { "$ref": "#/definitions/args_option" },
        "assert": {
          "anyOf": [{ "type": "boolean" }, { "type": "integer" }],
          "default": false,
          "description": "A boolean indicating if the task will fail when the result of the\nexpression is falsy. If an integer is given, the task will exit with that\ncode when the result is falsy."
        },
        "capture_stdout": {
          "description": "Redirects the task output to a file with the given path. Supports\nenvironment variable interpolation.",
          "type": "string"
        },
        "case": {
          "anyOf": [
            {
              "anyOf": [
                { "type": "string" },
                { "type": "integer" },
                { "type": "boolean" }
              ]
            },
            {
              "items": {
                "anyOf": [
                  { "type": "string" },
                  { "type": "integer" },
                  { "type": "boolean" }
                ]
              },
              "type": "array"
            }
          ]
        },
        "cwd": {
          "description": "Specify the current working directory that this task should run with. This\ncan be a relative path from the project root or an absolute path, and\nenvironment variables can be used in the format ${VAR_NAME}.",
          "pattern": "\\S",
          "type": "string"
        },
        "deps": {
          "description": "A list of task invocations that will be executed before this one. Each item\nin the list is a reference to another task defined within the tasks object.",
          "items": { "type": "string" },
          "type": "array"
        },
        "env": { "$ref": "#/definitions/env_option" },
        "envfile": { "$ref": "#/definitions/envfile_option" },
        "executor": { "$ref": "#/definitions/executor_task_option" },
        "expr": {
          "description": "Evaluates a Python expression to produce a result. Can reference\nenvironment variables and arguments, and leverage most python builtins.",
          "type": "string"
        },
        "help": {
          "description": "Help text to be displayed next to the task name in the documentation when\npoe is run without specifying a task.",
          "type": "string"
        },
        "ignore_fail": {
          "anyOf": [
            { "type": "boolean" },
            { "items": { "type": "integer" }, "type": "array" }
          ],
          "default": false,
          "description": "Return exit code 0 even if the task fails, or specify a list of task exit\ncodes to ignore."
        },
        "imports": {
          "description": "A list of Python modules to be imported for use in the expression.",
          "items": { "type": "string" },
          "type": "array"
        },
        "use_exec": {
          "default": false,
          "description": "Specify that this task should be executed in the same process, instead of\nas a subprocess. Note: This feature has limitations, such as not being\ncompatible with tasks that are referenced by other tasks and not working on\nWindows.",
          "type": "boolean"
        },
        "uses": {
          "additionalProperties": { "type": "string" },
          "description": "Allows this task to use the output of other tasks which are executed first.\nThe values are references to the names of the tasks, and the keys are\nenvironment variables by which the results of those tasks will be\naccessible in this task.",
          "type": "object"
        },
        "verbosity": {
          "description": "Specify the verbosity level for this task, from -2 (least verbose) to 2\n(most verbose), overriding the project level verbosity setting, which\ndefaults to 0.",
          "enum": [-2, -1, 0, 1, 2],
          "type": "integer"
        }
      },
      "required": ["expr"],
      "then": { "properties": { "capture_stdout": false } },
      "type": "object"
    },
    "groups_map": {
      "additionalProperties": false,
      "patternProperties": {
        "^[\\w-]+$": { "$ref": "#/definitions/task_group" }
      },
      "type": "object"
    },
    "include_script": {
      "anyOf": [
        { "type": "string" },
        {
          "items": {
            "anyOf": [
              { "type": "string" },
              { "$ref": "#/definitions/include_script_item" }
            ]
          },
          "type": "array"
        }
      ]
    },
    "include_script_item": {
      "additionalProperties": false,
      "properties": {
        "cwd": {
          "description": "Specify the working directory for resolving relative paths referenced by the\nincluded script.",
          "pattern": "\\S",
          "type": "string"
        },
        "executor": { "$ref": "#/definitions/executor_task_option" },
        "script": {
          "description": "A reference to a Python callable that returns toml or json-like config to be\nmerged into the project config.",
          "type": "string"
        }
      },
      "required": ["script"],
      "type": "object"
    },
    "parallel_task": {
      "additionalProperties": false,
      "properties": {
        "args": { "$ref": "#/definitions/args_option" },
        "cwd": {
          "description": "Specify the current working directory that this task should run with. This\ncan be a relative path from the project root or an absolute path, and\nenvironment variables can be used in the format ${VAR_NAME}.",
          "pattern": "\\S",
          "type": "string"
        },
        "default_item_type": {
          "description": "Change the default item type that strings in the parallel task are\ninterpreted as. By default this matches the project-level\n`default_array_item_task_type` setting.",
          "type": "string"
        },
        "deps": {
          "description": "A list of task invocations that will be executed before this one. Each item\nin the list is a reference to another task defined within the tasks object.",
          "items": { "type": "string" },
          "type": "array"
        },
        "env": { "$ref": "#/definitions/env_option" },
        "envfile": { "$ref": "#/definitions/envfile_option" },
        "executor": { "$ref": "#/definitions/executor_task_option" },
        "help": {
          "description": "Help text to be displayed next to the task name in the documentation when\npoe is run without specifying a task.",
          "type": "string"
        },
        "ignore_fail": {
          "default": false,
          "description": "If set, the parallel task will continue running even if one of the subtasks\nfails.",
          "enum": [true, false, "return_zero", "return_non_zero"]
        },
        "output_mode": {
          "default": "stream",
          "description": "Controls how leaf-task stdout is emitted: either streamed line-by-line\nor buffered output on task completion.",
          "enum": ["stream", "buffer"],
          "type": "string"
        },
        "parallel": {
          "description": "Runs an array of subtasks concurrently. Each subtask runs in its own\nsubprocess; output lines are interleaved and prefixed with the subtask\nname by default.",
          "items": {
            "allOf": [
              { "$ref": "#/definitions/task_def" },
              {
                "if": { "type": "object" },
                "then": { "properties": { "args": false }, "type": "object" }
              }
            ]
          },
          "type": "array"
        },
        "prefix": {
          "anyOf": [
            { "type": "string" },
            { "enum": [false], "type": "boolean" }
          ],
          "default": "{name}",
          "description": "Set the prefix applied to each line of output from subtasks. By default\nthis is the task name. Set to false to disable prefixing."
        },
        "prefix_max": {
          "default": 16,
          "description": "Set the maximum width of the prefix. Longer prefixes will be truncated.",
          "type": "integer"
        },
        "prefix_template": {
          "default": "{color_start}{prefix}{color_end} | ",
          "description": "Specifies a template for how the prefix is applied after truncating it to\nthe prefix_max length.",
          "type": "string"
        },
        "uses": {
          "additionalProperties": { "type": "string" },
          "description": "Allows this task to use the output of other tasks which are executed first.\nThe values are references to the names of the tasks, and the keys are\nenvironment variables by which the results of those tasks will be\naccessible in this task.",
          "type": "object"
        },
        "verbosity": {
          "description": "Specify the verbosity level for this task, from -2 (least verbose) to 2\n(most verbose), overriding the project level verbosity setting, which\ndefaults to 0.",
          "enum": [-2, -1, 0, 1, 2],
          "type": "integer"
        }
      },
      "required": ["parallel"],
      "type": "object"
    },
    "parallel_task_with_case": {
      "additionalProperties": false,
      "properties": {
        "args": { "$ref": "#/definitions/args_option" },
        "case": {
          "anyOf": [
            {
              "anyOf": [
                { "type": "string" },
                { "type": "integer" },
                { "type": "boolean" }
              ]
            },
            {
              "items": {
                "anyOf": [
                  { "type": "string" },
                  { "type": "integer" },
                  { "type": "boolean" }
                ]
              },
              "type": "array"
            }
          ]
        },
        "cwd": {
          "description": "Specify the current working directory that this task should run with. This\ncan be a relative path from the project root or an absolute path, and\nenvironment variables can be used in the format ${VAR_NAME}.",
          "pattern": "\\S",
          "type": "string"
        },
        "default_item_type": {
          "description": "Change the default item type that strings in the parallel task are\ninterpreted as. By default this matches the project-level\n`default_array_item_task_type` setting.",
          "type": "string"
        },
        "deps": {
          "description": "A list of task invocations that will be executed before this one. Each item\nin the list is a reference to another task defined within the tasks object.",
          "items": { "type": "string" },
          "type": "array"
        },
        "env": { "$ref": "#/definitions/env_option" },
        "envfile": { "$ref": "#/definitions/envfile_option" },
        "executor": { "$ref": "#/definitions/executor_task_option" },
        "help": {
          "description": "Help text to be displayed next to the task name in the documentation when\npoe is run without specifying a task.",
          "type": "string"
        },
        "ignore_fail": {
          "default": false,
          "description": "If set, the parallel task will continue running even if one of the subtasks\nfails.",
          "enum": [true, false, "return_zero", "return_non_zero"]
        },
        "output_mode": {
          "default": "stream",
          "description": "Controls how leaf-task stdout is emitted: either streamed line-by-line\nor buffered output on task completion.",
          "enum": ["stream", "buffer"],
          "type": "string"
        },
        "parallel": {
          "description": "Runs an array of subtasks concurrently. Each subtask runs in its own\nsubprocess; output lines are interleaved and prefixed with the subtask\nname by default.",
          "items": {
            "allOf": [
              { "$ref": "#/definitions/task_def" },
              {
                "if": { "type": "object" },
                "then": { "properties": { "args": false }, "type": "object" }
              }
            ]
          },
          "type": "array"
        },
        "prefix": {
          "anyOf": [
            { "type": "string" },
            { "enum": [false], "type": "boolean" }
          ],
          "default": "{name}",
          "description": "Set the prefix applied to each line of output from subtasks. By default\nthis is the task name. Set to false to disable prefixing."
        },
        "prefix_max": {
          "default": 16,
          "description": "Set the maximum width of the prefix. Longer prefixes will be truncated.",
          "type": "integer"
        },
        "prefix_template": {
          "default": "{color_start}{prefix}{color_end} | ",
          "description": "Specifies a template for how the prefix is applied after truncating it to\nthe prefix_max length.",
          "type": "string"
        },
        "uses": {
          "additionalProperties": { "type": "string" },
          "description": "Allows this task to use the output of other tasks which are executed first.\nThe values are references to the names of the tasks, and the keys are\nenvironment variables by which the results of those tasks will be\naccessible in this task.",
          "type": "object"
        },
        "verbosity": {
          "description": "Specify the verbosity level for this task, from -2 (least verbose) to 2\n(most verbose), overriding the project level verbosity setting, which\ndefaults to 0.",
          "enum": [-2, -1, 0, 1, 2],
          "type": "integer"
        }
      },
      "required": ["parallel"],
      "type": "object"
    },
    "ref_task": {
      "additionalProperties": false,
      "properties": {
        "args": { "$ref": "#/definitions/args_option" },
        "capture_stdout": {
          "description": "Redirects the task output to a file with the given path. Supports\nenvironment variable interpolation.",
          "type": "string"
        },
        "cwd": {
          "description": "Specify the current working directory that this task should run with. This\ncan be a relative path from the project root or an absolute path, and\nenvironment variables can be used in the format ${VAR_NAME}.",
          "pattern": "\\S",
          "type": "string"
        },
        "deps": {
          "description": "A list of task invocations that will be executed before this one. Each item\nin the list is a reference to another task defined within the tasks object.",
          "items": { "type": "string" },
          "type": "array"
        },
        "env": { "$ref": "#/definitions/env_option" },
        "envfile": { "$ref": "#/definitions/envfile_option" },
        "help": {
          "description": "Help text to be displayed next to the task name in the documentation when\npoe is run without specifying a task.",
          "type": "string"
        },
        "ignore_fail": {
          "default": false,
          "description": "If true the failure of the referenced task will be ignored and the ref task\nwill return exit code 0.",
          "type": "boolean"
        },
        "ref": {
          "description": "Invokes another task by name, with or without arguments.",
          "type": "string"
        },
        "uses": {
          "additionalProperties": { "type": "string" },
          "description": "Allows this task to use the output of other tasks which are executed first.\nThe values are references to the names of the tasks, and the keys are\nenvironment variables by which the results of those tasks will be\naccessible in this task.",
          "type": "object"
        },
        "verbosity": {
          "description": "Specify the verbosity level for this task, from -2 (least verbose) to 2\n(most verbose), overriding the project level verbosity setting, which\ndefaults to 0.",
          "enum": [-2, -1, 0, 1, 2],
          "type": "integer"
        }
      },
      "required": ["ref"],
      "type": "object"
    },
    "ref_task_with_case": {
      "additionalProperties": false,
      "properties": {
        "args": { "$ref": "#/definitions/args_option" },
        "capture_stdout": {
          "description": "Redirects the task output to a file with the given path. Supports\nenvironment variable interpolation.",
          "type": "string"
        },
        "case": {
          "anyOf": [
            {
              "anyOf": [
                { "type": "string" },
                { "type": "integer" },
                { "type": "boolean" }
              ]
            },
            {
              "items": {
                "anyOf": [
                  { "type": "string" },
                  { "type": "integer" },
                  { "type": "boolean" }
                ]
              },
              "type": "array"
            }
          ]
        },
        "cwd": {
          "description": "Specify the current working directory that this task should run with. This\ncan be a relative path from the project root or an absolute path, and\nenvironment variables can be used in the format ${VAR_NAME}.",
          "pattern": "\\S",
          "type": "string"
        },
        "deps": {
          "description": "A list of task invocations that will be executed before this one. Each item\nin the list is a reference to another task defined within the tasks object.",
          "items": { "type": "string" },
          "type": "array"
        },
        "env": { "$ref": "#/definitions/env_option" },
        "envfile": { "$ref": "#/definitions/envfile_option" },
        "help": {
          "description": "Help text to be displayed next to the task name in the documentation when\npoe is run without specifying a task.",
          "type": "string"
        },
        "ignore_fail": {
          "default": false,
          "description": "If true the failure of the referenced task will be ignored and the ref task\nwill return exit code 0.",
          "type": "boolean"
        },
        "ref": {
          "description": "Invokes another task by name, with or without arguments.",
          "type": "string"
        },
        "uses": {
          "additionalProperties": { "type": "string" },
          "description": "Allows this task to use the output of other tasks which are executed first.\nThe values are references to the names of the tasks, and the keys are\nenvironment variables by which the results of those tasks will be\naccessible in this task.",
          "type": "object"
        },
        "verbosity": {
          "description": "Specify the verbosity level for this task, from -2 (least verbose) to 2\n(most verbose), overriding the project level verbosity setting, which\ndefaults to 0.",
          "enum": [-2, -1, 0, 1, 2],
          "type": "integer"
        }
      },
      "required": ["ref"],
      "type": "object"
    },
    "script_task": {
      "additionalProperties": false,
      "allOf": [
        {
          "if": {
            "properties": { "use_exec": { "const": true } },
            "required": ["use_exec"]
          },
          "then": { "properties": { "capture_stdout": false } }
        },
        {
          "if": {
            "properties": {
              "script": { "pattern": "^[^:]*$", "type": "string" }
            },
            "required": ["script"]
          },
          "then": { "properties": { "print_result": false } }
        }
      ],
      "properties": {
        "args": { "$ref": "#/definitions/args_option" },
        "capture_stdout": {
          "description": "Redirects the task output to a file with the given path. Supports\nenvironment variable interpolation.",
          "type": "string"
        },
        "cwd": {
          "description": "Specify the current working directory that this task should run with. This\ncan be a relative path from the project root or an absolute path, and\nenvironment variables can be used in the format ${VAR_NAME}.",
          "pattern": "\\S",
          "type": "string"
        },
        "deps": {
          "description": "A list of task invocations that will be executed before this one. Each item\nin the list is a reference to another task defined within the tasks object.",
          "items": { "type": "string" },
          "type": "array"
        },
        "env": { "$ref": "#/definitions/env_option" },
        "envfile": { "$ref": "#/definitions/envfile_option" },
        "executor": { "$ref": "#/definitions/executor_task_option" },
        "help": {
          "description": "Help text to be displayed next to the task name in the documentation when\npoe is run without specifying a task.",
          "type": "string"
        },
        "ignore_fail": {
          "anyOf": [
            { "type": "boolean" },
            { "items": { "type": "integer" }, "type": "array" }
          ],
          "default": false,
          "description": "Return exit code 0 even if the task fails, or specify a list of task exit\ncodes to ignore."
        },
        "print_result": {
          "default": false,
          "description": "If true then the return value of the Python callable will be output to\nstdout, unless it is None.",
          "type": "boolean"
        },
        "script": {
          "description": "Invokes a Python callable or module, optionally with values or expressions\npassed as arguments.",
          "examples": [
            "my_pkg.my_module",
            "my_pkg.my_module:main",
            "my_pkg.my_module:main(only='images', log_env={'LOG_PATH':'/var/log'})"
          ],
          "type": "string"
        },
        "use_exec": {
          "default": false,
          "description": "Specify that this task should be executed in the same process, instead of\nas a subprocess. Note: This feature has limitations, such as not being\ncompatible with tasks that are referenced by other tasks and not working on\nWindows.",
          "type": "boolean"
        },
        "uses": {
          "additionalProperties": { "type": "string" },
          "description": "Allows this task to use the output of other tasks which are executed first.\nThe values are references to the names of the tasks, and the keys are\nenvironment variables by which the results of those tasks will be\naccessible in this task.",
          "type": "object"
        },
        "verbosity": {
          "description": "Specify the verbosity level for this task, from -2 (least verbose) to 2\n(most verbose), overriding the project level verbosity setting, which\ndefaults to 0.",
          "enum": [-2, -1, 0, 1, 2],
          "type": "integer"
        }
      },
      "required": ["script"],
      "type": "object"
    },
    "script_task_with_case": {
      "additionalProperties": false,
      "allOf": [
        {
          "if": {
            "properties": { "use_exec": { "const": true } },
            "required": ["use_exec"]
          },
          "then": { "properties": { "capture_stdout": false } }
        },
        {
          "if": {
            "properties": {
              "script": { "pattern": "^[^:]*$", "type": "string" }
            },
            "required": ["script"]
          },
          "then": { "properties": { "print_result": false } }
        }
      ],
      "properties": {
        "args": { "$ref": "#/definitions/args_option" },
        "capture_stdout": {
          "description": "Redirects the task output to a file with the given path. Supports\nenvironment variable interpolation.",
          "type": "string"
        },
        "case": {
          "anyOf": [
            {
              "anyOf": [
                { "type": "string" },
                { "type": "integer" },
                { "type": "boolean" }
              ]
            },
            {
              "items": {
                "anyOf": [
                  { "type": "string" },
                  { "type": "integer" },
                  { "type": "boolean" }
                ]
              },
              "type": "array"
            }
          ]
        },
        "cwd": {
          "description": "Specify the current working directory that this task should run with. This\ncan be a relative path from the project root or an absolute path, and\nenvironment variables can be used in the format ${VAR_NAME}.",
          "pattern": "\\S",
          "type": "string"
        },
        "deps": {
          "description": "A list of task invocations that will be executed before this one. Each item\nin the list is a reference to another task defined within the tasks object.",
          "items": { "type": "string" },
          "type": "array"
        },
        "env": { "$ref": "#/definitions/env_option" },
        "envfile": { "$ref": "#/definitions/envfile_option" },
        "executor": { "$ref": "#/definitions/executor_task_option" },
        "help": {
          "description": "Help text to be displayed next to the task name in the documentation when\npoe is run without specifying a task.",
          "type": "string"
        },
        "ignore_fail": {
          "anyOf": [
            { "type": "boolean" },
            { "items": { "type": "integer" }, "type": "array" }
          ],
          "default": false,
          "description": "Return exit code 0 even if the task fails, or specify a list of task exit\ncodes to ignore."
        },
        "print_result": {
          "default": false,
          "description": "If true then the return value of the Python callable will be output to\nstdout, unless it is None.",
          "type": "boolean"
        },
        "script": {
          "description": "Invokes a Python callable or module, optionally with values or expressions\npassed as arguments.",
          "examples": [
            "my_pkg.my_module",
            "my_pkg.my_module:main",
            "my_pkg.my_module:main(only='images', log_env={'LOG_PATH':'/var/log'})"
          ],
          "type": "string"
        },
        "use_exec": {
          "default": false,
          "description": "Specify that this task should be executed in the same process, instead of\nas a subprocess. Note: This feature has limitations, such as not being\ncompatible with tasks that are referenced by other tasks and not working on\nWindows.",
          "type": "boolean"
        },
        "uses": {
          "additionalProperties": { "type": "string" },
          "description": "Allows this task to use the output of other tasks which are executed first.\nThe values are references to the names of the tasks, and the keys are\nenvironment variables by which the results of those tasks will be\naccessible in this task.",
          "type": "object"
        },
        "verbosity": {
          "description": "Specify the verbosity level for this task, from -2 (least verbose) to 2\n(most verbose), overriding the project level verbosity setting, which\ndefaults to 0.",
          "enum": [-2, -1, 0, 1, 2],
          "type": "integer"
        }
      },
      "required": ["script"],
      "type": "object"
    },
    "sequence_task": {
      "additionalProperties": false,
      "properties": {
        "args": { "$ref": "#/definitions/args_option" },
        "cwd": {
          "description": "Specify the current working directory that this task should run with. This\ncan be a relative path from the project root or an absolute path, and\nenvironment variables can be used in the format ${VAR_NAME}.",
          "pattern": "\\S",
          "type": "string"
        },
        "default_item_type": {
          "description": "Change the default item type that strings in the sequence are interpreted\nas.",
          "type": "string"
        },
        "deps": {
          "description": "A list of task invocations that will be executed before this one. Each item\nin the list is a reference to another task defined within the tasks object.",
          "items": { "type": "string" },
          "type": "array"
        },
        "env": { "$ref": "#/definitions/env_option" },
        "envfile": { "$ref": "#/definitions/envfile_option" },
        "executor": { "$ref": "#/definitions/executor_task_option" },
        "help": {
          "description": "Help text to be displayed next to the task name in the documentation when\npoe is run without specifying a task.",
          "type": "string"
        },
        "ignore_fail": {
          "default": false,
          "description": "If set, the sequence will continue running even if one of the tasks fails.",
          "enum": [true, false, "return_zero", "return_non_zero"]
        },
        "sequence": {
          "description": "Runs an array of subtasks one after the other. Items may be inline task\ndefinitions, task references by name. Nested arrays are run as parallel\ntasks.",
          "items": {
            "allOf": [
              { "$ref": "#/definitions/task_def" },
              {
                "if": { "type": "object" },
                "then": { "properties": { "args": false }, "type": "object" }
              }
            ]
          },
          "type": "array"
        },
        "uses": {
          "additionalProperties": { "type": "string" },
          "description": "Allows this task to use the output of other tasks which are executed first.\nThe values are references to the names of the tasks, and the keys are\nenvironment variables by which the results of those tasks will be\naccessible in this task.",
          "type": "object"
        },
        "verbosity": {
          "description": "Specify the verbosity level for this task, from -2 (least verbose) to 2\n(most verbose), overriding the project level verbosity setting, which\ndefaults to 0.",
          "enum": [-2, -1, 0, 1, 2],
          "type": "integer"
        }
      },
      "required": ["sequence"],
      "type": "object"
    },
    "sequence_task_with_case": {
      "additionalProperties": false,
      "properties": {
        "args": { "$ref": "#/definitions/args_option" },
        "case": {
          "anyOf": [
            {
              "anyOf": [
                { "type": "string" },
                { "type": "integer" },
                { "type": "boolean" }
              ]
            },
            {
              "items": {
                "anyOf": [
                  { "type": "string" },
                  { "type": "integer" },
                  { "type": "boolean" }
                ]
              },
              "type": "array"
            }
          ]
        },
        "cwd": {
          "description": "Specify the current working directory that this task should run with. This\ncan be a relative path from the project root or an absolute path, and\nenvironment variables can be used in the format ${VAR_NAME}.",
          "pattern": "\\S",
          "type": "string"
        },
        "default_item_type": {
          "description": "Change the default item type that strings in the sequence are interpreted\nas.",
          "type": "string"
        },
        "deps": {
          "description": "A list of task invocations that will be executed before this one. Each item\nin the list is a reference to another task defined within the tasks object.",
          "items": { "type": "string" },
          "type": "array"
        },
        "env": { "$ref": "#/definitions/env_option" },
        "envfile": { "$ref": "#/definitions/envfile_option" },
        "executor": { "$ref": "#/definitions/executor_task_option" },
        "help": {
          "description": "Help text to be displayed next to the task name in the documentation when\npoe is run without specifying a task.",
          "type": "string"
        },
        "ignore_fail": {
          "default": false,
          "description": "If set, the sequence will continue running even if one of the tasks fails.",
          "enum": [true, false, "return_zero", "return_non_zero"]
        },
        "sequence": {
          "description": "Runs an array of subtasks one after the other. Items may be inline task\ndefinitions, task references by name. Nested arrays are run as parallel\ntasks.",
          "items": {
            "allOf": [
              { "$ref": "#/definitions/task_def" },
              {
                "if": { "type": "object" },
                "then": { "properties": { "args": false }, "type": "object" }
              }
            ]
          },
          "type": "array"
        },
        "uses": {
          "additionalProperties": { "type": "string" },
          "description": "Allows this task to use the output of other tasks which are executed first.\nThe values are references to the names of the tasks, and the keys are\nenvironment variables by which the results of those tasks will be\naccessible in this task.",
          "type": "object"
        },
        "verbosity": {
          "description": "Specify the verbosity level for this task, from -2 (least verbose) to 2\n(most verbose), overriding the project level verbosity setting, which\ndefaults to 0.",
          "enum": [-2, -1, 0, 1, 2],
          "type": "integer"
        }
      },
      "required": ["sequence"],
      "type": "object"
    },
    "shell_task": {
      "additionalProperties": false,
      "properties": {
        "args": { "$ref": "#/definitions/args_option" },
        "capture_stdout": {
          "description": "Redirects the task output to a file with the given path. Supports\nenvironment variable interpolation.",
          "type": "string"
        },
        "cwd": {
          "description": "Specify the current working directory that this task should run with. This\ncan be a relative path from the project root or an absolute path, and\nenvironment variables can be used in the format ${VAR_NAME}.",
          "pattern": "\\S",
          "type": "string"
        },
        "deps": {
          "description": "A list of task invocations that will be executed before this one. Each item\nin the list is a reference to another task defined within the tasks object.",
          "items": { "type": "string" },
          "type": "array"
        },
        "env": { "$ref": "#/definitions/env_option" },
        "envfile": { "$ref": "#/definitions/envfile_option" },
        "executor": { "$ref": "#/definitions/executor_task_option" },
        "help": {
          "description": "Help text to be displayed next to the task name in the documentation when\npoe is run without specifying a task.",
          "type": "string"
        },
        "ignore_fail": {
          "anyOf": [
            { "type": "boolean" },
            { "items": { "type": "integer" }, "type": "array" }
          ],
          "default": false,
          "description": "Return exit code 0 even if the task fails, or specify a list of task exit\ncodes to ignore."
        },
        "interpreter": {
          "anyOf": [
            {
              "enum": [
                "posix",
                "sh",
                "bash",
                "zsh",
                "fish",
                "pwsh",
                "powershell",
                "python"
              ],
              "type": "string"
            },
            {
              "items": {
                "enum": [
                  "posix",
                  "sh",
                  "bash",
                  "zsh",
                  "fish",
                  "pwsh",
                  "powershell",
                  "python"
                ],
                "type": "string"
              },
              "minItems": 1,
              "type": "array"
            }
          ],
          "description": "Specify the shell interpreter that this task should execute with, or a list\nof interpreters in order of preference."
        },
        "shell": {
          "description": "Executes the content as a shell scripts inside a new shell interpreter.\nNormally the bash interpreter to used unless specified otherwise.",
          "examples": [
            "echo 'Hello World'",
            "cat foo.txt | grep bar",
            "for i in {1..5}; do echo \"Welcome $i times\"; done"
          ],
          "type": "string"
        },
        "uses": {
          "additionalProperties": { "type": "string" },
          "description": "Allows this task to use the output of other tasks which are executed first.\nThe values are references to the names of the tasks, and the keys are\nenvironment variables by which the results of those tasks will be\naccessible in this task.",
          "type": "object"
        },
        "verbosity": {
          "description": "Specify the verbosity level for this task, from -2 (least verbose) to 2\n(most verbose), overriding the project level verbosity setting, which\ndefaults to 0.",
          "enum": [-2, -1, 0, 1, 2],
          "type": "integer"
        }
      },
      "required": ["shell"],
      "type": "object"
    },
    "shell_task_with_case": {
      "additionalProperties": false,
      "properties": {
        "args": { "$ref": "#/definitions/args_option" },
        "capture_stdout": {
          "description": "Redirects the task output to a file with the given path. Supports\nenvironment variable interpolation.",
          "type": "string"
        },
        "case": {
          "anyOf": [
            {
              "anyOf": [
                { "type": "string" },
                { "type": "integer" },
                { "type": "boolean" }
              ]
            },
            {
              "items": {
                "anyOf": [
                  { "type": "string" },
                  { "type": "integer" },
                  { "type": "boolean" }
                ]
              },
              "type": "array"
            }
          ]
        },
        "cwd": {
          "description": "Specify the current working directory that this task should run with. This\ncan be a relative path from the project root or an absolute path, and\nenvironment variables can be used in the format ${VAR_NAME}.",
          "pattern": "\\S",
          "type": "string"
        },
        "deps": {
          "description": "A list of task invocations that will be executed before this one. Each item\nin the list is a reference to another task defined within the tasks object.",
          "items": { "type": "string" },
          "type": "array"
        },
        "env": { "$ref": "#/definitions/env_option" },
        "envfile": { "$ref": "#/definitions/envfile_option" },
        "executor": { "$ref": "#/definitions/executor_task_option" },
        "help": {
          "description": "Help text to be displayed next to the task name in the documentation when\npoe is run without specifying a task.",
          "type": "string"
        },
        "ignore_fail": {
          "anyOf": [
            { "type": "boolean" },
            { "items": { "type": "integer" }, "type": "array" }
          ],
          "default": false,
          "description": "Return exit code 0 even if the task fails, or specify a list of task exit\ncodes to ignore."
        },
        "interpreter": {
          "anyOf": [
            {
              "enum": [
                "posix",
                "sh",
                "bash",
                "zsh",
                "fish",
                "pwsh",
                "powershell",
                "python"
              ],
              "type": "string"
            },
            {
              "items": {
                "enum": [
                  "posix",
                  "sh",
                  "bash",
                  "zsh",
                  "fish",
                  "pwsh",
                  "powershell",
                  "python"
                ],
                "type": "string"
              },
              "minItems": 1,
              "type": "array"
            }
          ],
          "description": "Specify the shell interpreter that this task should execute with, or a list\nof interpreters in order of preference."
        },
        "shell": {
          "description": "Executes the content as a shell scripts inside a new shell interpreter.\nNormally the bash interpreter to used unless specified otherwise.",
          "examples": [
            "echo 'Hello World'",
            "cat foo.txt | grep bar",
            "for i in {1..5}; do echo \"Welcome $i times\"; done"
          ],
          "type": "string"
        },
        "uses": {
          "additionalProperties": { "type": "string" },
          "description": "Allows this task to use the output of other tasks which are executed first.\nThe values are references to the names of the tasks, and the keys are\nenvironment variables by which the results of those tasks will be\naccessible in this task.",
          "type": "object"
        },
        "verbosity": {
          "description": "Specify the verbosity level for this task, from -2 (least verbose) to 2\n(most verbose), overriding the project level verbosity setting, which\ndefaults to 0.",
          "enum": [-2, -1, 0, 1, 2],
          "type": "integer"
        }
      },
      "required": ["shell"],
      "type": "object"
    },
    "switch_task": {
      "additionalProperties": false,
      "properties": {
        "args": { "$ref": "#/definitions/args_option" },
        "capture_stdout": {
          "description": "Redirects the task output to a file with the given path. Supports\nenvironment variable interpolation.",
          "type": "string"
        },
        "control": {
          "description": "A required definition for a task to be executed to determine which case\ntask to run.",
          "oneOf": [
            { "type": "string" },
            { "$ref": "#/definitions/expr_task" },
            { "$ref": "#/definitions/cmd_task" },
            { "$ref": "#/definitions/script_task" }
          ]
        },
        "cwd": {
          "description": "Specify the current working directory that this task should run with. This\ncan be a relative path from the project root or an absolute path, and\nenvironment variables can be used in the format ${VAR_NAME}.",
          "pattern": "\\S",
          "type": "string"
        },
        "default": {
          "default": "fail",
          "description": "Defines the default behavior if no cases are matched. Can either pass or\nfail.",
          "enum": ["pass", "fail"],
          "type": "string"
        },
        "deps": {
          "description": "A list of task invocations that will be executed before this one. Each item\nin the list is a reference to another task defined within the tasks object.",
          "items": { "type": "string" },
          "type": "array"
        },
        "env": { "$ref": "#/definitions/env_option" },
        "envfile": { "$ref": "#/definitions/envfile_option" },
        "executor": { "$ref": "#/definitions/executor_task_option" },
        "help": {
          "description": "Help text to be displayed next to the task name in the documentation when\npoe is run without specifying a task.",
          "type": "string"
        },
        "switch": {
          "description": "Runs one of several subtasks based on the output of a control task.\nThe control task is executed first; its output is matched against\neach case to select which subtask to run.",
          "items": {
            "allOf": [
              { "$ref": "#/definitions/task_def_with_case" },
              {
                "if": { "type": "object" },
                "then": { "properties": { "args": false }, "type": "object" }
              },
              {
                "if": { "type": "object" },
                "then": { "properties": { "uses": false }, "type": "object" }
              },
              {
                "if": { "type": "object" },
                "then": { "properties": { "deps": false }, "type": "object" }
              }
            ]
          },
          "type": "array"
        },
        "uses": {
          "additionalProperties": { "type": "string" },
          "description": "Allows this task to use the output of other tasks which are executed first.\nThe values are references to the names of the tasks, and the keys are\nenvironment variables by which the results of those tasks will be\naccessible in this task.",
          "type": "object"
        },
        "verbosity": {
          "description": "Specify the verbosity level for this task, from -2 (least verbose) to 2\n(most verbose), overriding the project level verbosity setting, which\ndefaults to 0.",
          "enum": [-2, -1, 0, 1, 2],
          "type": "integer"
        }
      },
      "required": ["control", "switch"],
      "type": "object"
    },
    "switch_task_with_case": {
      "additionalProperties": false,
      "properties": {
        "args": { "$ref": "#/definitions/args_option" },
        "capture_stdout": {
          "description": "Redirects the task output to a file with the given path. Supports\nenvironment variable interpolation.",
          "type": "string"
        },
        "case": {
          "anyOf": [
            {
              "anyOf": [
                { "type": "string" },
                { "type": "integer" },
                { "type": "boolean" }
              ]
            },
            {
              "items": {
                "anyOf": [
                  { "type": "string" },
                  { "type": "integer" },
                  { "type": "boolean" }
                ]
              },
              "type": "array"
            }
          ]
        },
        "control": {
          "description": "A required definition for a task to be executed to determine which case\ntask to run.",
          "oneOf": [
            { "type": "string" },
            { "$ref": "#/definitions/expr_task" },
            { "$ref": "#/definitions/cmd_task" },
            { "$ref": "#/definitions/script_task" }
          ]
        },
        "cwd": {
          "description": "Specify the current working directory that this task should run with. This\ncan be a relative path from the project root or an absolute path, and\nenvironment variables can be used in the format ${VAR_NAME}.",
          "pattern": "\\S",
          "type": "string"
        },
        "default": {
          "default": "fail",
          "description": "Defines the default behavior if no cases are matched. Can either pass or\nfail.",
          "enum": ["pass", "fail"],
          "type": "string"
        },
        "deps": {
          "description": "A list of task invocations that will be executed before this one. Each item\nin the list is a reference to another task defined within the tasks object.",
          "items": { "type": "string" },
          "type": "array"
        },
        "env": { "$ref": "#/definitions/env_option" },
        "envfile": { "$ref": "#/definitions/envfile_option" },
        "executor": { "$ref": "#/definitions/executor_task_option" },
        "help": {
          "description": "Help text to be displayed next to the task name in the documentation when\npoe is run without specifying a task.",
          "type": "string"
        },
        "switch": {
          "description": "Runs one of several subtasks based on the output of a control task.\nThe control task is executed first; its output is matched against\neach case to select which subtask to run.",
          "items": {
            "allOf": [
              { "$ref": "#/definitions/task_def_with_case" },
              {
                "if": { "type": "object" },
                "then": { "properties": { "args": false }, "type": "object" }
              },
              {
                "if": { "type": "object" },
                "then": { "properties": { "uses": false }, "type": "object" }
              },
              {
                "if": { "type": "object" },
                "then": { "properties": { "deps": false }, "type": "object" }
              }
            ]
          },
          "type": "array"
        },
        "uses": {
          "additionalProperties": { "type": "string" },
          "description": "Allows this task to use the output of other tasks which are executed first.\nThe values are references to the names of the tasks, and the keys are\nenvironment variables by which the results of those tasks will be\naccessible in this task.",
          "type": "object"
        },
        "verbosity": {
          "description": "Specify the verbosity level for this task, from -2 (least verbose) to 2\n(most verbose), overriding the project level verbosity setting, which\ndefaults to 0.",
          "enum": [-2, -1, 0, 1, 2],
          "type": "integer"
        }
      },
      "required": ["control", "switch"],
      "type": "object"
    },
    "task_def": {
      "oneOf": [
        { "type": "string" },
        { "items": { "$ref": "#/definitions/task_def" }, "type": "array" },
        { "$ref": "#/definitions/cmd_task" },
        { "$ref": "#/definitions/expr_task" },
        { "$ref": "#/definitions/parallel_task" },
        { "$ref": "#/definitions/ref_task" },
        { "$ref": "#/definitions/script_task" },
        { "$ref": "#/definitions/sequence_task" },
        { "$ref": "#/definitions/shell_task" },
        { "$ref": "#/definitions/switch_task" },
        {
          "additionalProperties": true,
          "properties": {
            "cmd": false,
            "expr": false,
            "parallel": false,
            "ref": false,
            "script": false,
            "sequence": false,
            "shell": false,
            "switch": false
          },
          "type": "object"
        }
      ]
    },
    "task_def_with_case": {
      "oneOf": [
        { "$ref": "#/definitions/cmd_task_with_case" },
        { "$ref": "#/definitions/expr_task_with_case" },
        { "$ref": "#/definitions/parallel_task_with_case" },
        { "$ref": "#/definitions/ref_task_with_case" },
        { "$ref": "#/definitions/script_task_with_case" },
        { "$ref": "#/definitions/sequence_task_with_case" },
        { "$ref": "#/definitions/shell_task_with_case" },
        { "$ref": "#/definitions/switch_task_with_case" },
        {
          "additionalProperties": true,
          "properties": {
            "cmd": false,
            "expr": false,
            "parallel": false,
            "ref": false,
            "script": false,
            "sequence": false,
            "shell": false,
            "switch": false
          },
          "type": "object"
        }
      ]
    },
    "task_group": {
      "additionalProperties": false,
      "properties": {
        "executor": { "$ref": "#/definitions/executor_task_option" },
        "heading": {
          "description": "A human-readable name for the group displayed in the help output.",
          "type": "string"
        },
        "tasks": { "$ref": "#/definitions/tasks_map" }
      },
      "required": [],
      "type": "object"
    },
    "tasks_map": {
      "additionalProperties": false,
      "patternProperties": {
        "^[^\\W\\d][\\w:+-]*$": { "$ref": "#/definitions/task_def" }
      },
      "type": "object"
    }
  },
  "description": "Poe the Poet is a task runner that integrates well with uv, poetry, or any project using pyproject.toml",
  "properties": {
    "default_array_item_task_type": {
      "default": "ref",
      "description": "When a task is declared as a string inside an array (e.g. inline in a\nsequence task), then it is interpreted as the default array item task type,\nwhich will be 'ref' unless otherwise specified.",
      "enum": ["cmd", "expr", "ref", "script", "shell"],
      "type": "string"
    },
    "default_array_task_type": {
      "default": "sequence",
      "description": "When a task is declared as an array (instead of a table), then it is\ninterpreted as the default array task type, which will be 'sequence' unless\notherwise specified.",
      "enum": ["parallel", "sequence"],
      "type": "string"
    },
    "default_task_type": {
      "default": "cmd",
      "description": "Sets the default task type for tasks defined as strings. By default, tasks\nare interpreted as shell commands ('cmd'). This can be overridden to\n'script' or other supported types.",
      "enum": ["cmd", "expr", "ref", "script", "shell"],
      "type": "string"
    },
    "env": {
      "$ref": "#/definitions/env_option",
      "description": "A map of environment variables to be set for all tasks."
    },
    "envfile": {
      "$ref": "#/definitions/envfile_option",
      "description": "Provide one or more env files to be loaded before running tasks. If an\narray is provided, files will be loaded in the given order."
    },
    "executor": {
      "$ref": "#/definitions/executor_option",
      "description": "Configure the executor for running tasks. The type can be 'auto', 'poetry',\n'uv', 'virtualenv', or 'simple', with 'auto' being the default. Some\nexecutor types accept additional configuration options."
    },
    "groups": {
      "$ref": "#/definitions/groups_map",
      "description": "Define groups of tasks to be displayed together in the help output."
    },
    "include": {
      "anyOf": [
        { "type": "string" },
        {
          "items": {
            "anyOf": [
              { "type": "string" },
              {
                "additionalProperties": false,
                "properties": {
                  "cwd": {
                    "description": "Specify the working directory for resolving relative paths referenced by the\nincluded config.",
                    "pattern": "\\S",
                    "type": "string"
                  },
                  "path": {
                    "description": "The path to the toml or json config file to include, relative to the parent\nconfig file.",
                    "type": "string"
                  },
                  "recursive": {
                    "description": "If true, includes declared in the included file will also be loaded.",
                    "type": "boolean"
                  }
                },
                "required": ["path"],
                "type": "object"
              }
            ]
          },
          "type": "array"
        }
      ],
      "description": "Specify one or more other toml or json files to load tasks from."
    },
    "include_script": {
      "$ref": "#/definitions/include_script",
      "description": "Load dynamically generated tasks from one or more python functions."
    },
    "poetry_command": {
      "default": "poe",
      "description": "Change the name of the task poe registers with poetry when used as a\nplugin.",
      "type": "string"
    },
    "poetry_hooks": {
      "additionalProperties": { "type": "string" },
      "description": "Register tasks to run automatically before or after other poetry CLI\ncommands.",
      "type": "object"
    },
    "shell_interpreter": {
      "anyOf": [
        {
          "enum": [
            "posix",
            "sh",
            "bash",
            "zsh",
            "fish",
            "pwsh",
            "powershell",
            "python"
          ],
          "type": "string"
        },
        {
          "items": {
            "enum": [
              "posix",
              "sh",
              "bash",
              "zsh",
              "fish",
              "pwsh",
              "powershell",
              "python"
            ],
            "type": "string"
          },
          "type": "array"
        }
      ],
      "default": "posix",
      "description": "Change the default shell interpreter for executing shell tasks. Normally,\ntasks are executed using a posix shell, but this can be overridden here."
    },
    "tasks": {
      "$ref": "#/definitions/tasks_map",
      "description": "A mapping of task names to task definitions."
    },
    "verbosity": {
      "default": 0,
      "description": "Sets the default verbosity level for all commands. '-1' is quieter, '0' is\nthe default level, and '1' is more verbose. The command line arguments are\nincremental, with '--quiet' or '-q' decreasing verbosity, and '--verbose'\nor '-v' increasing it.",
      "enum": [-2, -1, 0, 1, 2],
      "type": "integer"
    }
  },
  "required": [],
  "title": "Poe the Poet configuration",
  "type": "object"
}
