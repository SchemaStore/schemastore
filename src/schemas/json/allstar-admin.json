{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://json.schemastore.org/allstar-admin.json",
  "title": "Allstar Repository Administrators policy configuration",
  "type": "object",
  "definitions": {
    "AdministratorExemption": {
      "type": "object",
      "properties": {
        "repo": {
          "description": "GitHub repository name. Globs are allowed.",
          "type": "string"
        },
        "ownerlessAllowed": {
          "description": "Whether repositories are allowed to have no administrators.",
          "type": "boolean",
          "default": false
        },
        "userAdminsAllowed": {
          "description": "Whether users are allowed to be admins on the repository. If false then only teams can be admins.",
          "type": "boolean",
          "default": true
        },
        "userAdmins": {
          "type": "array",
          "description": "Specific users allowed to administer this repository. Overrides the boolean setting userAdminsAllowed.",
          "items": {
            "type": "string"
          }
        },
        "maxNumberUserAdmins": {
          "description": "Maximum number of users with admin permissions allowed on this repository. Takes effect only if greater than zero.",
          "type": "integer"
        },
        "teamAdminsAllowed": {
          "description": "Whether teams are allowed to be admins on the repository. If false then only users can be admins.",
          "type": "boolean",
          "default": true
        },
        "teamAdmins": {
          "type": "array",
          "description": "Specific teams allowed to administer this repository. Overrides the boolean setting teamAdminsAllowed.",
          "items": {
            "type": "string"
          }
        },
        "maxNumberAdminTeams": {
          "description": "Maximum number of teams with admin permissions allowed on this repository. Takes effect only if greater than zero.",
          "type": "integer"
        }
      },
      "additionalProperties": false,
      "required": []
    },
    "OrgConfig": {
      "type": "object",
      "properties": {
        "optConfig": {
          "$ref": "#/definitions/OrgOptConfig"
        },
        "action": {
          "description": "Which action to take. Default log; other options include issue.",
          "type": "string",
          "default": "log"
        },
        "ownerlessAllowed": {
          "description": "Whether repositories are allowed to have no administrators.",
          "type": "boolean",
          "default": false
        },
        "userAdminsAllowed": {
          "description": "Whether users are allowed to be admins on a repository. If false then only teams can be admins.",
          "type": "boolean",
          "default": true
        },
        "maxNumberUserAdmins": {
          "description": "Maximum number of users with admin permissions allowed on a repository. Takes effect only if greater than zero.",
          "type": "integer"
        },
        "teamAdminsAllowed": {
          "description": "Whether teams are allowed to be admins on a repository. If false then only users can be admins.",
          "type": "boolean",
          "default": true
        },
        "maxNumberAdminTeams": {
          "description": "Maximum number of teams with admin permissions allowed on a repository. Takes effect only if greater than zero.",
          "type": "integer"
        },
        "exemptions": {
          "type": "array",
          "description": "List of exemption entries for repositories and admin allowances. Only defined at org level.",
          "items": {
            "$ref": "#/definitions/AdministratorExemption"
          }
        },
        "baseConfig": {
          "$ref": "#/definitions/BaseConfig"
        }
      },
      "required": ["optConfig", "action"],
      "additionalProperties": false
    },
    "RepoConfig": {
      "type": "object",
      "properties": {
        "optConfig": {
          "$ref": "#/definitions/RepoOptConfig"
        },
        "action": {
          "description": "Override for org-level action when present.",
          "type": "string"
        },
        "ownerlessAllowed": {
          "description": "Override for org-level ownerlessAllowed when present.",
          "type": "boolean"
        },
        "userAdminsAllowed": {
          "description": "Override for org-level userAdminsAllowed when present.",
          "type": "boolean"
        },
        "maxNumberUserAdmins": {
          "description": "Override for org-level maxNumberUserAdmins when present.",
          "type": "integer"
        },
        "teamAdminsAllowed": {
          "description": "Override for org-level teamAdminsAllowed when present.",
          "type": "boolean"
        },
        "maxNumberAdminTeams": {
          "description": "Override for org-level maxNumberAdminTeams when present.",
          "type": "integer"
        }
      },
      "additionalProperties": false,
      "required": []
    },
    "OrgOptConfig": {
      "type": "object",
      "description": "OptConfig contains the opt in/out configuration.",
      "properties": {
        "optOutStrategy": {
          "$ref": "#/definitions/OptOutStrategy"
        },
        "optInRepos": {
          "$ref": "#/definitions/OptInRepos"
        },
        "optOutRepos": {
          "$ref": "#/definitions/OptOutRepos"
        },
        "optOutPrivateRepos": {
          "$ref": "#/definitions/OptOutPrivateRepos"
        },
        "optOutPublicRepos": {
          "$ref": "#/definitions/OptOutPublicRepos"
        },
        "optOutArchivedRepos": {
          "$ref": "#/definitions/OptOutArchivedRepos"
        },
        "optOutForkedRepos": {
          "$ref": "#/definitions/OptOutForkedRepos"
        },
        "disableRepoOverride": {
          "$ref": "#/definitions/DisableRepoOverride"
        }
      },
      "required": [],
      "additionalProperties": false
    },
    "DisableRepoOverride": {
      "type": "boolean",
      "description": "DisableRepoOverride : set to true to disallow repos from opt-in/out in their config."
    },
    "OptOutForkedRepos": {
      "type": "boolean",
      "description": "OptOutForkedRepos : set to true to opt-out forked repositories."
    },
    "OptOutArchivedRepos": {
      "type": "boolean",
      "description": "OptOutArchivedRepos : set to true to opt-out archived repositories."
    },
    "OptOutPublicRepos": {
      "type": "boolean",
      "description": "OptOutPublicRepos : set to true to not access public repos."
    },
    "OptOutPrivateRepos": {
      "type": "boolean",
      "description": "OptOutPrivateRepos : set to true to not access private repos."
    },
    "OptOutRepos": {
      "type": "array",
      "description": "OptOutRepos is the list of repos to opt-out when in opt-out strategy.",
      "items": {
        "type": "string"
      }
    },
    "OptInRepos": {
      "type": "array",
      "description": "OptInRepos is the list of repos to opt-in when in opt-in strategy.",
      "items": {
        "type": "string"
      }
    },
    "OptOutStrategy": {
      "type": "boolean",
      "description": "OptOutStrategy : set to true to change from opt-in to opt-out."
    },
    "RepoOptConfig": {
      "type": "object",
      "description": "Repository-level opt-in and opt-out settings.",
      "properties": {
        "optIn": {
          "type": "boolean",
          "description": "Opt in this repository when the organization uses opt-in strategy."
        },
        "optOut": {
          "type": "boolean",
          "description": "Opt out this repository when the organization uses opt-out strategy."
        }
      },
      "required": [],
      "additionalProperties": false
    },
    "BaseConfig": {
      "type": "string",
      "description": "The GitHub owner/repository containing the base configuration. The file at the same path is merged first, then values in this file override it. See https://github.com/ossf/allstar#org-level-base-and-merge-configuration-location.",
      "pattern": "^[^/]+/[^/]+$",
      "maxLength": 140
    }
  },
  "anyOf": [
    {
      "$ref": "#/definitions/OrgConfig"
    },
    {
      "$ref": "#/definitions/RepoConfig"
    }
  ]
}
