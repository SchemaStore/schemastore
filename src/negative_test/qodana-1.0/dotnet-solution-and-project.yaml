# yaml-language-server: $schema=../../schemas/json/qodana-1.0.json
version: '1.0'

dotnet:
  # Error: The properties 'solution' and 'project' cannot be specified
  # at the same time.
  solution: 'Name of the Solution'
  project: 'Name of the Project'
