#:schema ../../schemas/json/modelsdev.json

config_version = -1

[agents]
tracked = ["codex", "codex"]

[[agents.custom]]
name = ""
repo = "not-a-repo"
agent_type = "desktop"
version_command = ["models", ""]

[display]
default_tab = "unknown"

[aliases]
agents = ""
