{
  "on": {
    "push": null
    },
  "jobs": {
    "one": {
      "runs-on": "ubuntu-latest",
      "defaults": {
        "run": { "shell": "invalid-value"}
      },
      "steps": [
        {
          "run": "echo 'Hello from ${{ runner.os }}'"
        }
      ]
    }
  }
}
