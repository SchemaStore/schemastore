{
  "logsBucket": "LOGS_BUCKET_LOCATION",
  "options": {
    "logging": "GCS_ONLY"
  },
  "serviceAccount": "INCORRECT_SERVICE_ACCOUNT_NAME",
  "steps": [
    {
      "args": ["echo", "Hello world!"],
      "name": "bash"
    }
  ]
}
