# yaml-language-server: $schema=../../schemas/json/metricshub.json
# MetricsHub Configuration

# Default logger level
loggerLevel: notaloglevel

# Internal collect interval
collectPeriod: 72z

otel:
  otel.metrics.exporter: otlps
  otel.exporter.otlp.metrics.endpoint: 1
  otel.exporter.otlp.metrics.protocol: 2

resourceGroups:
