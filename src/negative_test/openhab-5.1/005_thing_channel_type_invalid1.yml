# yaml-language-server: $schema=../../schemas/json/openhab-5.1.json

version: 1

things:
  # If type is set kind can not be set
  my:thing:uuid:
    channels:
      channel_1:
        type: switch
        kind: state

