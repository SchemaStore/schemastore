# yaml-language-server: $schema=../../schemas/json/bxci.schema-3.x.json
project:
  name: any

stages:
  single_stage:

  parallel_stages:
    parallel:
      parallel_stage_one:
        steps:
          - one
      parallel_stage_two:
        steps:
          - two
