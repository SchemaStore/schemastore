project:
  name: any

config:
  cache:
    repository:
      address: https://binary-repository.com/directory
      credentials: SOME_CREDENTIALS

    items:
      - type: unknown
        enabled: true

stages:
  first:
    steps:
      - echo "Hello"
