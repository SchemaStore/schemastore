# yaml-language-server: $schema=../../schemas/json/bxci.schema-2.x.json
project:
  name: any

config:
  build:
    checkmarx:
      exclusionsSetting: invalid

stages:
  first:
    steps:
      - echo "Hello"
